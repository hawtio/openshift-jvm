machine:
  node:
    version: v6.1.0

branches:
  ignore:
    - builds

dependencies:
  pre:
    - npm install -g bower gulp
    - npm update
    - bower update
    - rm -Rf site/*
  override:

test:
  override:
    - echo yes

deployment:
  website:
    branch: /.*?/
    owner: hawtio
    commands:
      - bash maybeDeploy.sh