machine:
  node:
    version: 0.10.33
  services:

branches:
  ignore:
    - builds

dependencies:
  pre:
    - npm install -g bower gulp
    - npm update
    - bower update
  override:

test:
  override:
    - echo yes

deployment:
  website:
    branch: master
    owner: hawtio
    commands:
      - bash maybeDeploy.sh


